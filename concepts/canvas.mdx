---
title: "Canvas files"
description: "JSON Canvas files for spatial diagrams — nodes, edges, and optional auto-layout from the wiki-link graph."
icon: "diagram-project"
---

Canvas files are JSON documents with a **`.canvas.json`** suffix. They store `nodes` and `edges` for infinite-canvas style layouts in the web UI and for agents via API or MCP.

## File format

Each canvas is JSON with top-level `nodes` and `edges` arrays. Node and edge objects carry `id`, `type`, optional coordinates (`x`, `y`), and a `data` map for extensibility.

## Graph-assisted layout

Two layout paths exist:

- **`POST /api/kiwi/canvas/generate`** builds a new canvas from the **wiki-link graph** using layout modes (`hierarchical`, `radial`, `force`, or `circular`). You can restrict to a folder prefix so only part of the graph is laid out.
- **`POST /api/kiwi/canvas/auto-layout`** repositions nodes in an **existing** canvas using Graphviz engines (`dot`, `neato`, `fdp`, `circo`).

## Web UI

The web UI opens `.canvas.json` files in structured (React Flow) or freeform (Excalidraw) mode. See [Web UI — Canvas](/guides/web-ui#canvas).

## Related documentation

- [Canvas REST API](/api/canvas) — list, read, write, patch, query, auto-layout, generate
- [MCP](/concepts/mcp) — `kiwi_canvas_list`, `kiwi_canvas_read`, `kiwi_canvas_write`, `kiwi_canvas_generate`
