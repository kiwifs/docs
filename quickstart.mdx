---
title: "Quickstart"
description: "Install KiwiFS and start a knowledge server in under 2 minutes."
icon: "rocket"
---

## 1. Install

<Tabs>
  <Tab title="One-line install">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/kiwifs/kiwifs/main/install.sh | sh
    ```
  </Tab>
  <Tab title="Build from source">
    Requires Go 1.25+ and Node.js 20+:
    ```bash
    git clone https://github.com/kiwifs/kiwifs.git && cd kiwifs
    cd ui && npm install && npm run build && cd ..
    go build -o kiwifs .
    ```
  </Tab>
  <Tab title="Docker">
    ```bash
    docker build -t kiwifs .
    docker run -v ./knowledge:/data -p 3333:3333 kiwifs
    ```
  </Tab>
</Tabs>

## 2. Initialize a knowledge base

```bash
kiwifs init --template knowledge --root ./knowledge
```

This creates a structured knowledge base:

```
knowledge/
├── SCHEMA.md          # Agent instructions: ingest, query, lint
├── index.md           # Auto-maintained table of contents
├── log.md             # Append-only chronological record
├── concepts/          # One page per concept
├── entities/          # One page per named entity
└── reports/           # Chronological reports
```

Other templates: `wiki`, `runbook`, `research`, or start blank with `kiwifs init`.

## 3. Start the server

```bash
kiwifs serve --root ./knowledge
```

Open [http://localhost:3333](http://localhost:3333) for the web UI.

## 4. Write from an agent

<Tabs>
  <Tab title="Filesystem">
    ```bash
    echo "# Authentication\n\nOAuth2 + JWT..." > ./knowledge/concepts/auth.md
    ```
  </Tab>
  <Tab title="REST API">
    ```bash
    curl -X PUT 'localhost:3333/api/kiwi/file?path=concepts/auth.md' \
      -H "X-Actor: my-agent" \
      -d "# Authentication\n\nOAuth2 + JWT..."
    ```
  </Tab>
  <Tab title="MCP">
    ```json
    {
      "mcpServers": {
        "kiwifs": {
          "command": "kiwifs",
          "args": ["mcp", "--root", "/path/to/knowledge"]
        }
      }
    }
    ```
  </Tab>
</Tabs>

## 5. Browse in the web UI

Open `http://localhost:3333`. See your page rendered with wiki links, backlinks, table of contents, and graph view.

## Next steps

<CardGroup cols={2}>
  <Card title="Files as truth" icon="file" href="/concepts/files-as-truth">
    Understand the core design decision.
  </Card>
  <Card title="Import data" icon="download" href="/import/overview">
    Bring in data from PostgreSQL, CSV, MongoDB, and more.
  </Card>
  <Card title="Configure search" icon="magnifying-glass" href="/concepts/search">
    Set up full-text and vector search.
  </Card>
  <Card title="Deploy" icon="cloud" href="/deploy/docker">
    Run in Docker or Kubernetes.
  </Card>
</CardGroup>
